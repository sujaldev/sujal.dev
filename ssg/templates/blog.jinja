{% extends "two-column.jinja" %}
{% block title %}Blog{% endblock %}
{% set selected_tab = "blog" %}
{% block left %}
    <h1>Posts</h1>
    {% if posts %}
        <ul>
            {% for post in posts %}
                <li><a href="{{ post.url }}">{{ post.text }}</a></li>
            {% endfor %}
        </ul>
    {% else %}
        <p class="placeholder-text">There are no posts yet.</p>
    {% endif %}
{% endblock %}
